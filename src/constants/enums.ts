export enum SORT {
    DATE_ASC = 'date_asc',
    DATE_DESC = 'date_desc',
    ALPHABET = 'alphabet',
    STATUS_ACTIVE = 'status_active',
    STATUS_FINISHED = 'status_finished',
  };
  
  export enum PRIORITY {
    HIGH = 'high',
    MEDIUM = 'medium',
    LOW = 'low',
  };
  
  export enum STATUS {
    ACTIVE = 0,
    FINISHED = 1,
  };
  